"use client";
import React from "react";
import styles from "@/styles/components/nextBtn.module.scss";

export default function NextBtn() {
  return (
    <div className={styles.Btn}>
      <p>次へ</p>
    </div>
  );
}
